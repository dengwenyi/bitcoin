# Bitcoin 项目协作规则

## 适用范围

- 本文件适用于 `D:\work\bitcoin` 整个仓库。
- 本目录承接原 `E:\work\bitcoin-31.1` 的项目目标、分析文档和协作约定。
- 原目录基于 Bitcoin Core 31.1；本目录当前源码为 31.99 开发快照。旧文档、旧构建参数、源码位置和结论只作为历史基线，实施前必须在当前提交上重新核对。

## 项目目标

- 以现有 Bitcoin Core C++ `bitcoind` 为行为基线，渐进式实现最小全节点和 TCP/IP 协议栈思想的分层。
- TCP/IP 式分层强调窄接口、单向依赖、下层不理解上层业务，以及由 composition root 统一装配；不机械照搬 OSI 层数。
- 优先建立构建目标与接口边界，再做物理裁剪；不要先删除目录或大规模重写共识、链状态和 `net_processing.cpp`。
- 优先复用现有边界，例如 `Transport`、`V1Transport`、`V2Transport`、`NetEventsInterface` 和 `ValidationSignals`，但每次使用前须确认当前源码仍保持相同语义。

## 实施顺序

1. 保留并测量当前 `bitcoind` 行为基线。
2. 建立最小 CMake/Visual Studio 构建配置，区分“配置或生成成功”与“二进制编译成功”。
3. 拆分构建目标和依赖方向，不改变共识行为。
4. 引入窄的链验证 facade，再逐步隔离 peer 会话、区块同步和交易中继。
5. 替换 composition root 后，才删除已经确认无引用的代码。

## 验证规则

- 不能用源码文件变少、CMake 配置成功或解决方案生成成功代替编译与运行验证。
- 每个分层阶段都应对照行为基线检查：区块接受结果、链状态与重组、P2P 字节行为、磁盘持久化和崩溃恢复。
- 共识与策略必须分开：mempool、费率、RBF 和 relay policy 不得混入共识层。
- 修改高风险路径时保持小步提交，并清楚标注静态推断、编译证据、运行证据和仍未验证的部分。

## 文档和构建产物

- 后续代码解读、设计和验证报告默认放在 `D:\work\bitcoin\work`。
- `work\01` 至 `work\04` 是从 31.1 项目迁入的历史分析文档；引用当前源码前必须复核并按需更新。
- 旧目录的 `build-*`、vcpkg 缓存和其他生成物不得直接搬入本目录；应基于当前提交重新生成。
- 31.1 时期的最小 VS2022 配置曾使用 `Visual Studio 17 2022`、x64、v143，并关闭 GUI、钱包、CLI、测试等可选目标；这些参数在当前快照上只作参考。

## 仓库安全

- 保留用户已有的未提交改动和未跟踪文件，不执行 `git reset --hard`、`git clean` 或覆盖性恢复。
- 不把配置成功表述为编译成功，也不把本地编译成功表述为运行或网络同步验收成功。
- 未经用户明确要求，不提交、推送、创建或合并拉取请求。
